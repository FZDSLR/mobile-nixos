diff --git a/drivers/net/wireless/mediatek/mt76/mt7921/mac.c b/drivers/net/wireless/mediatek/mt76/mt7921/mac.c
index e69978184..956234748 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7921/mac.c
+++ b/drivers/net/wireless/mediatek/mt76/mt7921/mac.c
@@ -505,9 +505,14 @@ static void mt7921_mac_tx_free(struct mt792x_dev *dev, void *data, int len)
 		return;
 
 	for (i = 0; i < count; i++) {
-		u32 msdu, info = le32_to_cpu(tx_info[i]);
+		u32 msdu, info;
 		u8 stat;
 
+		if (WARN_ON_ONCE((void *)&tx_info[i] >= end))
+			return;
+
+		info = le32_to_cpu(tx_info[i]);
+
 		/* 1'b1: new wcid pair.
 		 * 1'b0: msdu_id with the same 'wcid pair' as above.
 		 */
@@ -515,7 +520,6 @@ static void mt7921_mac_tx_free(struct mt792x_dev *dev, void *data, int len)
 			struct mt792x_link_sta *mlink;
 			u16 idx;
 
-			count++;
 			idx = FIELD_GET(MT_TX_FREE_WLAN_ID, info);
 			wcid = mt76_wcid_ptr(dev, idx);
 			sta = wcid_to_sta(wcid);
